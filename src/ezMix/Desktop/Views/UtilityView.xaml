<UserControl x:Class="Desktop.Views.UtilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" HorizontalAlignment="Center" Width="760">

            <!-- Tiêu đề -->
            <TextBlock Text="🧰 Tiện ích xử lý đề kiểm tra" 
                       FontSize="24" FontWeight="Bold" Foreground="#FF9933" 
                       TextAlignment="Center" Margin="0,0,0,20"/>

            <Grid Margin="0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Grid.Column="0" Margin="5"
                        Content="Sửa lệch công thức MathType"
                        Style="{StaticResource ezPrimaryButton}"
                        Command="{Binding FixMathTypeCommand}"/>

                <TextBlock Grid.Row="0" Grid.Column="1" Margin="5"
                           Style="{StaticResource ezTextBlock}"
                           TextWrapping="Wrap"
                           Text="Căn chỉnh lại các công thức bị lệch do lỗi định dạng MathType khi copy từ Word. Giúp đề kiểm tra hiển thị chuẩn hơn khi in hoặc trộn."/>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>