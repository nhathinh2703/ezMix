<Window x:Class="Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:models="clr-namespace:Desktop.Models"
        xmlns:helpers="clr-namespace:Desktop.Helpers"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Phần mềm trộn đề ezMix"
        Height="950"
        Width="1600">

    <Window.Resources>
        <helpers:ViewTemplateSelector x:Key="ViewTemplateSelector" />

        <!-- Style cho nút menu -->
        <Style x:Key="SidebarMenuButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Hover -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFAA44"/>
                </Trigger>
                <!-- Focus -->
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="#FFAA44"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
                <!-- Selected -->
                <!--<DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="#FFAA44"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>-->
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding MenuWidth}"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- CỘT MENU -->
        <Grid Background="#FF9933" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo -->
            <Border BorderThickness="0,0,0,1" BorderBrush="White">
                <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0">
                    <TextBlock Text="🎓" Foreground="White" FontSize="22"/>
                    <TextBlock Text="ezMix" Foreground="White" FontSize="18" FontWeight="Bold" Margin="5,0,0,0" VerticalAlignment="Center"
                               Visibility="{Binding IsMenuExpanded, Converter={StaticResource BoolToVisibilityConverter}}" />
                </StackPanel>
            </Border>

            <!-- Danh sách menu -->
            <ItemsControl ItemsSource="{Binding Menus}" Grid.Row="1" Margin="0,20,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Margin" Value="0,4,0,4"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:MenuItem}">
                        <Button Command="{Binding DataContext.ChangeViewCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource SidebarMenuButton}">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Icon -->
                                <TextBlock Text="{Binding Icon}" FontSize="20" Width="28" TextAlignment="Center" Padding="5"/>
                                <!-- Title -->
                                <TextBlock Text="{Binding Title}" FontSize="15" VerticalAlignment="Center" Padding="5" Margin="5,0"
                                           Visibility="{Binding DataContext.IsMenuExpanded, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Grid.Row="2" Text="{Binding AppVersion}" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,10" />
        </Grid>

        <!-- CỘT PHẢI -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <!-- Thanh trên cùng -->
            <Border BorderThickness="0,0,0,1" Background="#FFF5EB" BorderBrush="#FFF5EB">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="☰" Background="Transparent" Foreground="#FF9933" BorderThickness="0" FontSize="16" Padding="10" Command="{Binding ToggleMenuCommand}"/>
                    <TextBlock Grid.Column="1" Text="{Binding CurrentTitle}"
                               Foreground="#FF9933" FontWeight="Bold" FontSize="14"
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Cùng nhau học hỏi, cùng nhau phát triển" Margin="10" FontSize="14"
                                    Foreground="#FF9933" FontStyle="Italic" FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Nội dung -->
            <ContentControl Grid.Row="1"
                            Content="{Binding CurrentViewModel}"
                            ContentTemplateSelector="{StaticResource ViewTemplateSelector}" />

            <!-- Footer -->
            <Border Grid.Row="2" Background="#FFF5EB" Padding="8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
                    <TextBlock Text="👤" Margin="0,0,5,0" Foreground="#FF9933"/>
                    <TextBlock Foreground="#FF9933">
                        <Run Text="Phát triển: "/>
                        <Run Text="Hà Quốc Bảo Nguyên" FontWeight="Bold"/>
                    </TextBlock>

                    <TextBlock Text="   "/>

                    <TextBlock Text="💬" Margin="15,0,5,0" Foreground="#FF9933"/>
                    <TextBlock Foreground="#FF9933">
                        <Run Text="Nhóm hỗ trợ: "/>
                        <Run Text="Zalo" FontWeight="Bold" TextDecorations="Underline" Cursor="Hand">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonUp">
                                    <i:InvokeCommandAction Command="{Binding OpenZaloCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Run>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
